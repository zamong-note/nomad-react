import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // 질문해주신 App.js 파일을 불러옵니다.

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);